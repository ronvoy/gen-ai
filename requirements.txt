# Runtime dependencies — everything the Flask app and the benchmark runners
# need on the server. Keep this list minimal: it installs into a shared-hosting
# virtualenv where wheels for compiled packages are often unavailable.
#
# Report and slide generation needs more than this; see requirements-dev.txt.

requests>=2.31.0
python-dotenv>=1.0.0
flask>=3.0.0
markdown>=3.5.0
